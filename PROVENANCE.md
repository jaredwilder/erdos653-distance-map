# Provenance

Focused-home extraction: 2026-09-14.

Surviving finite result:

- Pass-2 / Pass-3 campaign records for `erdos653-campaign-001` preserve the explicit four-point witness and certify the lower-bound component `g(4)>=3` by exact squared-distance comparisons.
- The matching upper bound is elementary: for four distinct points, every `R(x_i)` lies in `{1,2,3}`, so at most three different `R`-values can occur.

Correction authority:

- `jaredwilder/erdos-ore-findings/SEMANTIC_COURT_13_ERDOS653.md`
- Git blob: `09cb18832afb38e143fa3b1526155f9b4d703b33`

That audit explicitly kills the regular-polygon `g(n)<=floor(n/2)` inference while preserving `g(4)=3` as a separate valid result. This repository follows the corrected chronology rather than the earlier Pass-6 headline.
